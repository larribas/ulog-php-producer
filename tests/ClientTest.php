<?php

class ClientTest extends PHPUnit_Framework_TestCase
{
    public function testSendRequest()
    {




        /*
        $client = new ulog\Client($host, $port, $token);
        $producer = new ulog\Producer($client);

        $producer->produce([new ulog\Event($streamName, $type, $version, $partitionKey, $time, $content)]);

        // n times
        $producer->queue(new ulog\Event($streamName, $type, $version, $partitionKey, $time, $content));
        $producer->produceQueue();
        $producer->clearQueue();
        */
    }
}
